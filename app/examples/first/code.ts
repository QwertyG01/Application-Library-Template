import { vabene } from "../../lib";

console.log("Ciao ");

declare type Ciao = {
  ok: string;
};

const hello: Ciao = {
  ok: "come va",
};

console.log(hello.ok);

vabene.Mappa;

// const files = fs.readdirSync("../../examples");
// console.log(files);
